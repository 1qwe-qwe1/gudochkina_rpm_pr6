<Page x:Class="gudochkina_pr3.Pages.RecyclingActsList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:gudochkina_pr3.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RecyclingActsList" Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="Акты приема-передачи отходов" 
                   FontSize="20" FontWeight="Bold" 
                   Margin="10" HorizontalAlignment="Center"/>

        <!-- Список актов -->
        <ListView x:Name="lvActs" Grid.Row="1" 
                  Margin="10"
                  MouseDoubleClick="lvActs_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="№ акта" Width="80" DisplayMemberBinding="{Binding ActId}"/>
                    <GridViewColumn Header="Дата" Width="100" DisplayMemberBinding="{Binding ActDateFormatted}"/>
                    <GridViewColumn Header="Контрагент" Width="200" DisplayMemberBinding="{Binding CounterpartyName}"/>
                    <GridViewColumn Header="Тип отходов" Width="150" DisplayMemberBinding="{Binding WasteTypeName}"/>
                    <GridViewColumn Header="Объем" Width="80" DisplayMemberBinding="{Binding VolumeFormatted}"/>
                    <GridViewColumn Header="Сумма" Width="100" DisplayMemberBinding="{Binding CostFormatted}"/>
                </GridView>
            </ListView.View>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Height" Value="35"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- Статусная строка -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="tbStatus" Text="Готово"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Page>
